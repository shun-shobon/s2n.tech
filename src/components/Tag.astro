---
interface Props {
	tag: string;
}

const { tag } = Astro.props;
---

<a
	href={`/tags/${tag}`}
	class="rounded-full bg-background-secondary px-2 py-0.5 font-cursive leading-tight transition-colors before:content-['#'] hover:bg-background-tertiary"
	>{tag}</a
>
