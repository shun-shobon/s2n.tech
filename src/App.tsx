import React, { FC } from "react";
import { BrowserRouter } from "react-router-dom";

import "./App.scss";

const App: FC = () => (
  <BrowserRouter>
    <h1>Hello, world!</h1>
  </BrowserRouter>
);

export default App;
